# 设计说明

主任务是为“工厂循环水设备选型沟通”准备可核查的营销资料。先定义场景与客户角色，再分析来源和比较条件，最后把特点转换为潜在客户利益。避免将参数高低等同于客户价值。

事实是一条带来源ID、定位与不可覆盖摘录的记录；生成内容引用事实ID。Pydantic约束形状，程序校验引用存在和摘录精确匹配；人工审核语义支持。这三层检查互不替代。演示解析器是有限标签规则，模型模式才调用模型；两种模式在每个批次和导出中保留。

比较规则只安全换算明确的W/kW。轴输出与输入功率、清水20°C与介质40°C不可自动排序；两份CC-75资料冲突全部显示。未披露字段输出文字占位与警告。价值说明遵循“需求→特点→潜在利益→证据→条件”，不虚构成本节省比例。

项目快照及上传BLOB存SQLite，文件路径不由上传名决定。单用户范围内事务保存，模型调用先持久记录预算。结构化输出全量验证成功才保存批次，失败保留已有数据。无工具调用、无代码执行、无网络抓取。提示注入防护使用系统边界与引用校验；并不宣称任意恶意输入都能被语义层面识别。

输入变化使历史批次失效；事实编辑使衍生结果失效。营销内容改动先待审核，二次确认引用。保留原内容、修改内容、时间、备注、来源与批次。项目删除是真正清理项目记录与BLOB；单来源删除保留历史证据用于审计，UI与README明确说明。

首版以40,000字符硬上限替代隐式截断与复杂分块；用户明确看到覆盖范围，超限要求减少资料。缓存指纹覆盖输入、来源哈希、版本和模型配置。避免自动重试带来的重复费用。
